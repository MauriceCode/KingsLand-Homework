function main(word, strt, fnsh) {
    let results;
    let subStr = word.substr(strt, fnsh);
    console.log(subStr)
}

main("ASentance", 1, 8)

// let teststr = 'ASentance'

// let substr = teststr.substring (1, 8 + 1)
// console.log(substr)